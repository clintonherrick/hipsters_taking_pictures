<div class="landing-text">
  <h1>It's Like Polaroids, But Digital</h1>
  <h6>Actually It's Nothing Like Polaroids, But It's Still Digital</h6>

  <% if user_signed_in? %>
    <%= link_to 'See Some Pics Bro', pictures_path, class:"btn btn-primary signup-btn" %>
    <%= link_to 'Add Some Pics Bro', new_picture_path, class:"btn btn-default signup-btn" %>
  <% else %>
    <%= link_to 'Sign Up With Email', new_user_registration_path, class:"btn btn-primary signup-btn" %>
  <% end %>
</div>
